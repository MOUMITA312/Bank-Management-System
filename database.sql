create database bank;
use bank;

create table bank.account (
	name varchar(50),
    acno varchar(10),
    dob varchar(10),
    ad varchar(100),
    phn varchar(10),
    balance float,
    user_id varchar(50),
    Password varchar(20)
);